spring:
    cloud:
        config:
             name: application
             profile: calculate
             discovery:
                  enabled: true
                  serviceId: leasing-config-server

spring.datasource.driverClassName: oracle.jdbc.driver.OracleDriver
spring.datasource.url: jdbc:oracle:thin:@192.168.249.87:1521:ncdb
spring.datasource.password: ztnc
spring.datasource.username: ztnc

eureka:
  port:
    8800
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.port}/eureka/